[package]
name = "nischalcodesrust"
version = "0.1.0"
edition = "2024"

[dependencies]
ratatui = "0.26" # Or the latest version available
crossterm = { version = "0.27", features = ["events"] }
rand = "0.8"
